{
  "name": "spear-server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "npm run lint && npm run startdb && clear; node app",
    "lint": "./node_modules/.bin/eslint . --fix",
    "startdb": "mongod &",
    "reinstall": "cd .. && rm -rf ./spear-server && git clone https://github.com/JinhyeokFang/spear-server && cd spear-server && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^5.7.5",
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "eslint": "^5.16.0"
  }
}
